---
layout: single
title: Day 18 strchr 
---

strchr 는 문자열 내 일치하는 문자가 있는지 검사하는 함수이다.

예제로 사용법을 보여주겠다.

```{.cpp}
#include <stdio.h>
#include <string.h>

int main() {
	char str[] = "apple";
	char* ptr = strchr(str, 'a');

	printf("%c", *ptr);
}
```

위와 같이 apple 에 a가 포함되면 ptr 값으로 a를 갖게 된다. 
